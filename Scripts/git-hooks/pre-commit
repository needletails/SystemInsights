#!/bin/sh
# Installed by Scripts/install-git-hooks.sh — runs before every commit.
set -eu

ROOT_DIR=$(git rev-parse --show-toplevel 2>/dev/null) || exit 0
cd "$ROOT_DIR"
exec sh Scripts/verify-safe-to-commit.sh
